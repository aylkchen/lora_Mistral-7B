# FAQ 任务中 LoRA `r=8` 与 `r=16` 结果分析

这份文档用于整理同一 FAQ 任务下，两组 LoRA 秩参数实验结果的差异，方便后续复盘、写简历和准备面试。

## 实验背景

- 任务：`FAQ / 客服助手`
- 基座模型：`Mistral-7B-Instruct-v0.1`
- 训练方式：`LoRA + SFT`
- 评测集：`data/faq/heldout.jsonl`
- 评测样本数：`100`
- 运行环境：`A800`
- 对比对象：
  - `r=16`
  - `r=8`

默认理解为：

- 两组实验的数据集、任务场景、评测脚本保持一致
- 差异主要来自 LoRA 的秩参数不同

## 原始结果

### `r=16`

```json
{
  "task_name": "faq_support_lora",
  "mode": "faq",
  "num_examples": 100,
  "base_rouge_l": 0.1343,
  "adapter_rouge_l": 0.9733,
  "base_keyword_hit_rate": 0.3667,
  "adapter_keyword_hit_rate": 0.995,
  "manual_scores": {}
}
```

### `r=8`

```json
{
  "task_name": "faq_support_lora",
  "mode": "faq",
  "num_examples": 100,
  "base_rouge_l": 0.1229,
  "adapter_rouge_l": 0.9685,
  "base_keyword_hit_rate": 0.3633,
  "adapter_keyword_hit_rate": 0.99,
  "manual_scores": {}
}
```

## 对比表

| 指标 | `r=16` | `r=8` | 差值 |
| --- | ---: | ---: | ---: |
| `base_rouge_l` | 0.1343 | 0.1229 | +0.0114 |
| `adapter_rouge_l` | 0.9733 | 0.9685 | +0.0048 |
| `base_keyword_hit_rate` | 0.3667 | 0.3633 | +0.0034 |
| `adapter_keyword_hit_rate` | 0.9950 | 0.9900 | +0.0050 |

说明：

- 表中的差值按 `r=16 - r=8` 计算
- 重点应该关注 `adapter_*` 指标，因为这才代表微调后的模型表现

## 结果解读

### 1. LoRA 微调是有效的

无论 `r=8` 还是 `r=16`，微调后的 FAQ 模型都明显优于基座模型：

- `ROUGE-L` 从大约 `0.12~0.13` 提升到 `0.97` 左右
- `keyword_hit_rate` 从大约 `0.36` 提升到 `0.99` 左右

这说明：

- 基座模型虽然具备一般性指令跟随能力，但对当前 FAQ 窄域知识并不稳定
- 经过 SFT 后，模型已经能较稳定地生成接近标准答案的回复
- LoRA 在这个任务上足以完成高质量的领域适配

### 2. `r=16` 优于 `r=8`，但优势很小

在微调后的指标上：

- `adapter_rouge_l`：`0.9733 > 0.9685`
- `adapter_keyword_hit_rate`：`0.995 > 0.990`

结论是：

- `r=16` 当前是更优配置
- 但提升幅度只有 `0.0048` 和 `0.0050`
- 这种优势属于“略优”，不是“明显拉开差距”

换句话说，在当前 FAQ 任务上，`r=8` 已经学到了大部分模式，`r=16` 只是进一步把答案拟合得更完整、更稳定一些。

### 3. 当前任务已经接近饱和

`adapter_rouge_l` 接近 `0.97`，`adapter_keyword_hit_rate` 接近 `1.0`，通常意味着：

- 任务相对窄域
- 问答模式比较稳定
- 训练集与评测集风格接近
- 模型已经把主要规律学得比较充分

因此：

- 增加 LoRA 容量仍然有收益
- 但收益已经明显变小
- 继续把 `r` 提得更高，未必会带来同等比例的提升

## 为什么 `r=8` 和 `r=16` 差距不大

可能的原因有这几类：

### 1. FAQ 任务本身难度不高

你的 FAQ 数据属于窄域问答，答案结构相对固定，比如：

- 物流时效
- 退货窗口
- 保修期限
- 支付方式

这类任务对模型来说更像“学习模板化业务规则”，而不是开放式复杂推理，因此较小的 LoRA 容量也能学得不错。

### 2. 数据模式重复度较高

FAQ 数据通常会出现很多类似表达：

- 问法不同，但答案模式相似
- 产品名不同，但规则结构接近
- 同一类业务问题具有高度复用的措辞

这种情况下，`r=8` 往往已经能覆盖主要模式。

### 3. 评测集和训练任务分布接近

当前评测用的是 `heldout.jsonl`，虽然是单独准备的评测题，但仍然属于同一 FAQ 业务域。  
所以模型更容易把训练中学到的规律迁移到评测上。

## 需要注意的地方

### 1. `base` 指标存在轻微波动

理论上，如果两次对比完全一致，`base model` 的指标应该非常接近。  
你这里 `base_rouge_l` 和 `base_keyword_hit_rate` 有少量波动，通常说明评测存在采样随机性。

可能原因：

- 生成时开启了 `do_sample=True`
- 温度采样带来轻微波动

这不影响总体结论，但如果想做更严谨的对比实验，建议：

- 评测时将 `do_sample` 设为 `false`
- 或者每个配置重复跑 `3` 次，取平均值

### 2. `manual_scores` 还是空的

说明目前你已经完成了自动评测，但人工评测表还没有填写。  
因此现阶段最准确的表述是：

- `自动指标显示 r=16 略优于 r=8`

而不是：

- `已经通过完整人工评测证明 r=16 更优`

## 当前阶段的结论

### 如果追求最优结果

优先选 `r=16`，因为：

- `ROUGE-L` 更高
- `keyword hit rate` 更高
- 虽然提升不大，但当前结果中它最好

### 如果追求性价比

优先选 `r=8`，因为：

- 结果和 `r=16` 非常接近
- LoRA 容量更小
- 训练和存储成本通常更低
- 更适合作为“轻量化可用配置”

### 从实验结论角度怎么说

最稳妥的说法是：

> 在 FAQ 窄域问答任务上，`r=8` 和 `r=16` 均能显著提升模型表现，其中 `r=16` 略优于 `r=8`，但提升有限，说明较低秩的 LoRA 已经具备较高性价比。

## 面试表达建议

你可以这样讲：

> 我在同一 FAQ 任务上对比了 `r=8` 和 `r=16` 两组 LoRA 配置。结果显示，两者都能显著优于基座模型，说明 LoRA 微调有效；其中 `r=16` 在 ROUGE-L 和关键词覆盖率上略优，但提升幅度不大。我认为这是因为 FAQ 属于窄域、规则性较强的任务，`r=8` 已经可以覆盖主要模式，因此后续增大秩参数的收益开始减弱。这组实验说明我不只是会跑通训练，还会做参数对比和性价比分析。

## 后续建议

如果你还想把这部分实验做得更扎实，可以继续补三件事：

1. 做 deterministic evaluation
将评测时的 `do_sample` 改为 `false`，减少随机性，让 `r=8 vs r=16` 的对比更稳定。

2. 补人工评测
填写 `faq_manual_sheet.jsonl` 中的正确率标注，形成自动指标 + 人工指标的双重证据。

3. 记录训练成本
补充：
- 训练时长
- 显存占用
- adapter 文件大小

这样就可以进一步回答：

- `r=16` 略优多少
- 为了这点提升，额外花了多少训练和存储成本

## 一句话总结

在当前 FAQ 任务上，`r=16` 是效果最优配置，`r=8` 是更高性价比配置；两者差距不大，说明该任务对 LoRA 容量的需求有限，而 SFT + LoRA 的主要收益已经在较小秩设置下得到释放。
